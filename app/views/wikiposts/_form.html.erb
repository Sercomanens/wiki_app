<%= form_with(model: wikipost) do |form| %>
  <% if wikipost.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(wikipost.errors.count, "error") %> prohibited this wikipost from being saved:</h2>

      <ul>
        <% wikipost.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
